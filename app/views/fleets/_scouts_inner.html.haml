- @fleet.users.tagged.each do |user|
  %li= "#{user.tag.text} (#{user.char_name}) in #{user.solar_system_name}"
  -#    = "+ #{pluralize(count, "other")}" unless first
- if @fleet.users.tagged.count == 0
  %li Tag some pilots to have them show up here
