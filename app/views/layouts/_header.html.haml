#header
  %h1= link_to (image_tag('FleetTracker.png', :alt => "Fleet Tracker") + image_tag("beta.jpeg", :class => "beta", :alt => "Beta", :title => "Fleet Tracker is free while in beta!")), root_path
  %ul.navigation
    %li=link_to "Fleets", root_path
    %li=link_to "About", about_path
  - if @current_user && @current_user.fleet
    #ping(data-refresh="#{30*1000}" data-base-url="#{root_url}")
